import { sum, product } from '@blabla/library'

export const helloProduct = (a: number, b:  number) => `hello ${product(a, b)}`
export const helloSum = (a: number, b:  number) => `hello ${sum(a, b)}`
